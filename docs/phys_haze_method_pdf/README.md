# PhysHazeDiffusion Method PDF

This folder contains a LaTeX summary of the physical haze generation method.

Build with:

```bash
make
```

The default engine is `tectonic`, which was used to generate `main.pdf` in this environment.
You can override it if a full TeX Live installation is available:

```bash
make LATEX_ENGINE=lualatex
```
